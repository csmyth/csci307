<%= form_for(@red_flag) do |f| %>
  <% if @red_flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@red_flag.errors.count, "error") %> prohibited this red_flag from being saved:</h2>

      <ul>
      <% @red_flag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_id %><br />
    <%= f.number_field :client_id %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br />
    <%= f.number_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
